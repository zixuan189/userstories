const assert = require('assert')

describe('Simple Math Test', () => {
    it('should be admin', () => {
        assert.equal(1 + 1, 2)
    })
    it('should return 9', () => {
        assert.equal(3 * 3, 9)
    })
})